linters:
  enable:
    - unused
    - bodyclose
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - gocyclo

run:
  timeout: 2m
  tests: true

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0